
function getBuildFile(file: string) {
    // when all the files get built via esbuild, create a randomly generated name (to make it opsec)
    // this function will let you get a specific file
    // getBuildFile("data.js") => /public/97aHSd87H-build.js
    // 
}