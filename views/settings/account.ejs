
<div class="dashboard">
    <%- include("../components/$header") %>

    <%- include("../components/settings/$nav") %>

    <%- include("../components/settings/$banner") %>

    <div class="settings-real">
        <section class="settings-sec">
            <h2>Profile</h2>
            <form action="/api/v1/settings/profile" method="POST" class="settings-form">
                <label>
                    <span>Username</span>
                    <input type="text" name="username" maxlength="32" required value="<%= profile.username %>" autocomplete="username">
                </label>
                <label>
                    <span>Display Name</span>
                    <input type="text" name="displayName" maxlength="64" required value="<%= profile.displayName %>" autocomplete="nickname">
                </label>
                <label>
                    <span>Email</span>
                    <input type="email" name="email" required value="<%= profile.email %>" autocomplete="email">
                </label>
                
                <button type="submit">Save profile</button>
            </form>
        </section>

        <section>
            <h2>Goals</h2>

            <% if (goals.length) { %>
                <div class="settings-goals">
                    <% goals.forEach(goal => { %>
                        <div class="settings-goal">
                            <div>
                                <p class="settings-goal-title"><%= goal.periodLabel %> || <%= goal.targetText %></p>
                                <p class="settings-muted"><%= goal.progressText %> / <%= goal.targetText %> || <%= goal.scope %></p>
                            </div>
                            <form action="/api/v1/settings/goals/<%= goal.id %>/delete" method="POST">
                                <button type="submit" title="Delete goal" aria-label="Delete goal">
                                    <i class="fa-solid fa-trash"></i>
                                </button>
                            </form>
                        </div>
                    <% }) %>
                </div>
            <% } else { %>
                <p class="settings-muted">No goals yet</p>
            <% } %>

            <% if (goalsRemaining > 0) { %>
                <form action="/api/v1/settings/goals" class="settings-form settings-form-inline" method="POST">
                    <label>
                        <span>Hours</span>
                        <input type="number" name="hours" min="0.25" step="0.25" value="1" required>
                    </label>
                    <label>
                        <span>Period</span>
                        <select name="period" required>
                            <option value="day">Daily</option>
                            <option value="week">Weekly</option>
                            <option value="month">Monthly</option>
                        </select>
                    </label>
                    <label>
                        <p style="display: none;">im lazy</p>
                        <button type="submit">Add goal</button>
                    </label>
                </form>
            <% } else { %>
                <p class="settings-muted">Maximum of <%= goalsMax %> goals reached</p>
            <% } %>
        </section>
        <section class="settings-sec is-danger">
            <h2>Delete account</h2>
            <p class="settings-muted">Permenantly deletes your heartbeats, projects, goals and API keys</p>
            <form class="settings-form" action="/api/v1/settings/delete" method="POST">
                <label>
                    <span>Password</span>
                    <input type="password" name="password" required autocomplete="current-password">
                </label>

                <label>
                    <span>Type DELETE to confirm</span>
                    <input type="text" name="confirm" required autocomplete="off" placeholder="DELETE">
                </label>
                <button type="submit" class="is-danger">Delete my account</button>
            </form>
        </section>
    </div>
</div>